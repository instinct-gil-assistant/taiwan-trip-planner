import postgres from 'postgres';
const sql=postgres(process.env.DATABASE_URL,{ssl:'require'});
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,PUT,OPTIONS','Access-Control-Allow-Headers':'content-type,x-trip-secret','Content-Type':'application/json'};
export default async function handler(req){if(req.method==='OPTIONS')return new Response('',{headers:cors});if(req.headers.get('x-trip-secret')!==process.env.TRIP_SECRET)return new Response(JSON.stringify({error:'forbidden'}),{status:403,headers:cors});if(req.method==='GET'){const [r]=await sql`select data,updated_at from trip_state where trip_id='taiwan-gil-dana'`;return new Response(JSON.stringify(r),{headers:cors});}if(req.method==='PUT'){const data=await req.json();const [r]=await sql`insert into trip_state(trip_id,data,updated_at) values ('taiwan-gil-dana',${sql.json(data)},now()) on conflict(trip_id) do update set data=excluded.data,updated_at=now() returning data,updated_at`;return new Response(JSON.stringify(r),{headers:cors});}return new Response('',{status:405,headers:cors});}
